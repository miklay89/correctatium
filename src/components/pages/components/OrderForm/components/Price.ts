import styled from "styled-components";

export const SPriceWrapper = styled.div`
  display: inline-flex;
  align-items: flex-end;
`;

export const SPriceNumber = styled.div`
  font-size: 60px;
  font-weight: 100;
  font-stretch: normal;
  font-style: normal;
  line-height: 1.37;
  letter-spacing: -0.19px;
  color: #0068e4;
  text-align: right;
`;

export const SPriceCurrency = styled.div`
  color: #0068e4;
  margin-bottom: 13px;
`;
